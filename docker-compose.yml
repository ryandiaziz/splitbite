
services:
  redis:
    image: redis:alpine
    container_name: splitbite-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    restart: always

  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: splitbite-server
    ports:
      - "9000:9000"
    environment:
      - REDIS_URL=redis://redis:6379
      - APPLICATION_SECRET=splitbite-super-secure-and-long-secret-key-for-production-1234567890
    depends_on:
      - redis
    restart: always

  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    container_name: splitbite-client
    ports:
      - "8080:80"
    depends_on:
      - server
    restart: always

volumes:
  redis_data:
